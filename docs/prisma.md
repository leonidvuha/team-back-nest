```npx prisma migrate dev --name <init>``` - вместо инит подставляем название миграции
```npx prisma generate``` - можно один раз сделать при начале работы

```npx prisma migrate reset``` - пересоздаст бд в соответсвии